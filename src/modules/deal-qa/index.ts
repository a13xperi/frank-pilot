/**
 * deal-qa — hosted Deal-Room Q&A bot (Telegram). Compartment-masked, cited,
 * extractive answers from the curated deal corpus. See compartment-guard.ts for
 * the security core. Dark until DEAL_QA_ENABLED=true.
 */
export { default as dealTelegramWebhookRouter } from "./webhook";
