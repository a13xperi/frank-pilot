node_modules/
dist/
logs/
.env
*.log
.DS_Store

# Ralph AI tool state
.ralph/
.ralphrc

# BP-03b compliance tape stub ledger (runtime data; canonical BP-02 will replace this)
server/tape/*.ndjson
.env.production.local

# Raw scrape output from scripts/scrape-gpmglv.mjs (gitignored to avoid repo bloat)
# Synthesised audit .md files stay committed; raw HTML/headers live here locally only.
docs/intel/raw/
