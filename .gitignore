node_modules/
dist/
logs/
.env
*.log
.DS_Store

# Ralph AI tool state
.ralph/
.ralphrc

# BP-03b compliance tape stub ledger (runtime data; canonical BP-02 will replace this)
server/tape/*.ndjson
.env.production.local

# Intel raw snapshots (re-fetchable via scripts/scrape-gpmglv.mjs)
docs/intel/raw/

# Local scratch: statewide-housing map generator (depends on a local NHD LIHD
# xlsx not in the repo) + its superseded standalone HTML build. The production
# map lives in client-tenant/public/.
demo/
.claude/
.playwright-mcp/

# Screening backtest harness output — one directory per run; not source-controlled.
tmp/screening-backtest/
